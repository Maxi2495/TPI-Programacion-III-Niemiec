plugins {
    id 'java'
    id 'org.springframework.boot' version '3.2.5'
    id 'io.spring.dependency-management' version '1.1.4'
}

group = 'com.utn.foodstore'
version = '1.0.0'

java {
    sourceCompatibility = JavaVersion.VERSION_21
}

repositories {
    mavenCentral()
}

dependencies {
    // Motor web (Para crear las URLs)
    implementation 'org.springframework.boot:spring-boot-starter-web'

    // Spring Data JPA (Para no escribir SQL a mano)
    implementation 'org.springframework.boot:spring-boot-starter-data-jpa'

    // Validaciones (Para chequear precios > 0, mails validos, etc.)
    implementation 'org.springframework.boot:spring-boot-starter-validation'

    // Base de datos H2 (Archivo local)
    runtimeOnly 'com.h2database:h2'

    // Lombok (Para no escribir Getters y Setters)
    compileOnly 'org.projectlombok:lombok'
    annotationProcessor 'org.projectlombok:lombok'
    //Esta linea es para encriptar constraseñas
    implementation 'org.springframework.boot:spring-boot-starter-security'
}